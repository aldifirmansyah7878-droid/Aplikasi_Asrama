<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">
    <LinearLayout android:orientation="vertical" android:padding="16dp">
        <TextView android:id="@+id/tvPeriode" android:textSize="16sp" android:textStyle="bold" />
        <TextView android:id="@+id/tvJumlah" android:textSize="14sp" android:layout_marginTop="4dp" />
        <TextView android:id="@+id/tvMetode" android:textSize="14sp" />
        <TextView android:id="@+id/tvStatus" android:layout_marginTop="8dp" android:padding="4dp" android:textColor="#FFFFFF" />
        <Button android:id="@+id/btnLihatBukti" android:layout_marginTop="8dp" android:text="Lihat Bukti" style="@style/Widget.MaterialComponents.Button.TextButton" android:visibility="gone" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>